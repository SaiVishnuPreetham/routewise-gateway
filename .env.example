# ============================================================
# RouteWise AI Gateway — Environment Variables Template
# ============================================================
# Copy this file to .env and fill in your free-tier API keys.
# Both keys are free, no credit card required.
#
# Fast Model — Groq (groq.com → free API key)
GROQ_API_KEY=
#
# Capable Model — Google Gemini (aistudio.google.com → free API key)
GEMINI_API_KEY=